{% extends 'base.html.twig' %}

{% block title %}BisList{% endblock %}

{% block body %}
    <h1>BisList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bis_list.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bis_list.name }}</td>
            </tr>
            <tr>
                <th>CharacterClass</th>
                <td>{{ bis_list.characterClass }}</td>
            </tr>
            <tr>
                <th>Specialization</th>
                <td>{{ bis_list.specialization }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bis_list_index') }}">back to list</a>

    <a href="{{ path('app_bis_list_edit', {'id': bis_list.id}) }}">edit</a>

    {{ include('bis_list/_delete_form.html.twig') }}
{% endblock %}
